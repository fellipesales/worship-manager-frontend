# URL publica da API backend
NEXT_PUBLIC_API_URL=https://api.seudominio.com

# URL onde o frontend esta rodando (usada pelo NextAuth)
NEXTAUTH_URL=https://seudominio.com

# Chave secreta para criptografar tokens JWT do NextAuth (gere uma string aleatoria)
# Gerar com: openssl rand -base64 32
NEXTAUTH_SECRET=

# Google OAuth (opcional - necessario apenas se usar login com Google)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
